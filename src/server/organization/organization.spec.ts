describe('Organization', () => {
    describe('#createOrganization', () => {
        it('Should create organization');
        it('Should throw an error when organization already exists');
        it('Should throw an error when missing organization fields');
    })
});